import associationResolvers from "./association";

/**
 * Root Resolvers
 * merge all resolvers to single Array object and export it
 * 
 */
const resolvers = [
    associationResolvers
]

export default resolvers